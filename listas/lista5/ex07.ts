function areaL507(base: number, altura: number): number {
  return base * altura;
}

let baseL507: number = Number(prompt("base:"));
let alturaL507: number = Number(prompt("altura:"));
alert(`area = ${areaL507(baseL507, alturaL507)}`);

const exe9: number = Number(prompt("digite a area do terreno"));
const exe9B: number = Number(prompt("digite o valor por m²"));

const preTotal: number = exe9 * exe9B;

alert(`o valor é ${preTotal} .`);

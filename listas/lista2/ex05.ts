const num1: number = Number(prompt("digite um numero"));
const num2: number = Number(prompt("digite outro numero"));

if (num1 > num2) {
  alert(`o maior é ${num1}`);
} else {
  alert(`o maior é ${num2}`);
}

const numEnt: number = Number(prompt("digite uma nota"));

if (numEnt >= 18) {
  alert("entra");
} else if (numEnt >= 16) {
  alert(`Somente Acompanhado`);
} else if (numa < 16) {
  alert(`não entra`);
}

const pesoA: number = Number(prompt("Digite o seu peso em kg :"));
const alturaA: number = Number(prompt("Digite a sua altura em metros:"));

const imcA: number = peso / (altura * altura);

alert(`O seu IMC é: ${imc.toFixed(2)}`);
